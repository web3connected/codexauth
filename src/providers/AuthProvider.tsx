'use client';

import React from 'react';

// Simplified AuthProvider for demo - no external dependencies
export function AuthProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
