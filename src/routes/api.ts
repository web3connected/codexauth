/**
 * API Routes
 *
 * Loads all routes from api/zones/z*\/
 * Zone contract (methods, middleware, auth) comes from the directory name.
 *
 * To add an API route: drop a file in the correct zone directory.
 *   api/zones/z3/  ← api endpoints (GET, POST, DELETE, api middleware)
 *   api/zones/z4/  ← protected api endpoints (all methods, auth required)
 */

import type { CodexRoute } from './types';

// No API routes yet — add zone files under api/zones/z{n}/ as needed
export const apiRoutes: CodexRoute[] = [];
